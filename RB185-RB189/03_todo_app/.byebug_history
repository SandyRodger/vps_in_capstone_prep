continue
@storage.update_list_name(id, list_name)
list_name
id
continue
    result.map do |tuple|
      {id: tuple["id"], name: tuple["name"], todos: []}
    end
result = query(todo_sql, id)
query(todo_sql, id)
todo_sql = "SELECT * FROM todos WHERE list_id = $1"
tuple
continue
list[0][:todos].size
eval list
list[:todos].size
exec list
continue
list[:todos].size
eval list
list
exit
q
\list
d
list
ç
list
